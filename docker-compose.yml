---
services:
  gpassg:
    container_name: gpassg
    image: git.cadred.ru/chilyumov_fedor/gpassg:latest
    build:
      context: .
    restart: always
    command: "--host 0.0.0.0"
    ports:
    - "8108:8000"
    env_file:
    - .env
